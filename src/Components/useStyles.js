
import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        flexGrow: 1,
        textDecoration: 'none',
    },
    link: {
        color: '#00bcd4',
        textDecoration: 'none',
    },
});

export default useStyles;